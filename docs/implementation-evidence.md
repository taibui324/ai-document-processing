# Implementation evidence

Started 2026-09-06 23:43 Asia/Ho_Chi_Minh. Synthetic data only.

- Foundation: dedicated Git root verified; confidential PDF/DOCX inputs ignored.
- Node 24.20.0: `npm ci --ignore-scripts`, unit tests, and build passed. Commands run through `npm exec --yes --package=node@24.20.0 --` because the host default is Node 26.
- TDD: liveness failed with 404 before adding the controller, then passed. Configuration failed on missing defaults, then passed including real credential and lease/mode rejection.
- `opensrc path @nestjs/platform-express typeorm redis pdf-lib zod --cwd .` fetched pinned dependency source. Reviewed upload interception, explicit migrations, Redis offline queuing, and PDF structural/encryption defaults.
- Selected real model: `gemini-2.5-flash`, listed in [Google's model catalog](https://ai.google.dev/gemini-api/docs/models) on 2026-09-06. Live provider access has not been tested.

## PR scope — 2026-09-07

The user requested that standalone demo tooling be excluded from the PR. `scripts/demo.ts`, `compose.demo.yaml`, their npm commands, and `test/demo.spec.ts` are omitted. The mock services, synthetic upload fixture, and fault/recovery tests remain. Earlier local demo results below are historical evidence; the standalone commands are not part of this branch. The original local `main` branch preserves that work.

Verification after exclusion: `npm run lint`, `npm run typecheck`, `npm run build`, `npm test` (10 tests), `npm run test:integration` (9 tests), and `npm run test:e2e` (22 tests) passed on Node 24.20.0. Docker image build, Compose configuration, strict OpenSpec validation, and `git diff --cached --check` also passed. The reduced total of 41 tests reflects removal of the standalone demo argument test.

## Pre-PR verification — 2026-09-07

The worker, mock services, container runtime, recovery tests, and reviewer documentation are implemented. The historical checkpoint below is retained to distinguish earlier failures from the final results.

Commands used Node 24.20.0, with `npm exec --yes --package=node@24.20.0 --` on this host:

| Check | Result |
| --- | --- |
| `npm ci --ignore-scripts` | Passed; production image dependency installation also passed |
| `npm run lint` | Passed, including the now-present `scripts/` directory |
| `npm run typecheck` | Passed |
| `npm run build` | Passed locally and inside the multi-stage image |
| `npm test` | 11 tests passed in 10 suites |
| `npm run test:integration` | 9 tests passed in 6 suites against isolated MySQL/Redis |
| `npm run test:e2e` | 22 tests passed in 6 suites, including the executable worker |
| Compose configuration | Main, test, and combined demo configuration passed `config --quiet` |
| `docker compose build` and `docker compose up -d --wait` | Passed; migrations exited 0 and long-running services became healthy |
| Mermaid CLI 11.17.0 | All three diagrams in `architecture.md` rendered to SVG using local Chrome |
| README local links | All referenced local files exist |
| `openspec validate build-resilient-document-processing --strict --no-interactive` | Passed |
| `git diff --check` | Passed |

The 42 tests cover authentication and hostile uploads, atomic concurrent replay, exclusive claims and fencing, Retry-After and deadline limits, unusable-output non-delivery, bounded HTTP bodies and cancellation, Redis interruption/recovery, vendor uncertainty, safe logs, and restart at four workflow checkpoints. Tests run sequentially against `compose.test.yaml`; no live external services are called.

Selected observed TDD cycles:

- Readiness returned 404 before implementation, then correctly returned 200 or dependency-related 503.
- Swagger lacked response schemas; the contract assertion failed, then passed with explicit response contracts.
- Acceptance logging was missing; captured-log assertions failed, then passed without document, extraction, filename, or credential leakage.
- The restarted response-loss mock repeated its fault and needed three vendor attempts; the regression failed before removing the duplicate branch, then passed with two attempts and one receipt.
- A nearly expired lease still allowed a two-second HTTP request. The regression observed about 2,059 ms before the fix, then about 314 ms with a 300 ms remaining lease. Dispatch now uses the minimum of request timeout, stage time remaining, and lease time remaining; expired results remain fenced.

Container operational verification used a synthetic PDF generated by `node dist/scripts/fixture.js`. A real upload returned 202; Swagger JSON exposed the job routes. Stopping the worker during an open AI response persisted `AI_CANCELLED`; restarting against the healthy mock completed with AI/vendor attempts **2/1** and the original AI deadline. An ordinary restart of MySQL and Redis, followed by app startup, preserved the completed job and **one receipt**. Recorded job: `f8139d6e-0f51-442c-8176-d3ef00c0d232`. A CREATE TABLE command under the app DB user failed with MySQL error 1142, confirming DDL is denied.

All 11 named scenarios passed on the final image through the same compiled demo entry point used by `npm run demo -- <scenario>`; every observed status, attempt count, and receipt count matched [the failure runbook](failure-scenarios.md). The npm wrapper was also exercised for happy and response-loss demos. Final response-loss job `4a8a3963-8330-41c3-812d-aa26786f7cae` completed with AI/vendor attempts 1/2 and one receipt. The invalid-acknowledgement scenario exhausted six vendor attempts with one remote receipt, correctly leaving delivery unconfirmed. The fast profile exercises actual HTTP timeouts and persisted retries without waiting 15 minutes. The over-900-second policy test uses an injected clock, separately from real socket-cancellation tests.

Git history separates foundation, API/persistence, workflow/integrations, runtime/recovery, and final documentation. Publishable files were checked for common credential/private-key patterns, and tracked/history paths were checked for confidential PDF/DOCX or `.env` files; none matched. Public local-demo credentials are intentionally documented. These bounded checks are not a comprehensive security audit.

**Limits:** no live Gemini smoke or extraction-accuracy evaluation was run; the selected model still requires live account/model verification. Exactly-once vendor effects depend on the vendor's durable idempotency contract. This is a synthetic-data, single-caller demonstration, with the production limits documented in [architecture](architecture.md).

**Time accounting:** work began at 2026-09-06 23:43 Asia/Ho_Chi_Minh; final verification continued through 2026-09-07 17:28, about 17 hours 45 minutes of wall time including user pauses and resumed sessions. Active engineering effort was not separately measured; the eight-hour target cannot be claimed as met. Remaining time was spent on mandatory recovery, container, and final verification gates rather than optional features.

## Historical progress checkpoint — superseded by final verification

Checkpoint includes the document submission/status API, MySQL schema and repositories, lease/retry state operations, Redis provider gating, Gemini/vendor HTTP adapters, mock receipt persistence, and their current tests. The background processing loop, full mock service, and end-to-end demonstration are not yet complete. This is a progress snapshot, not a completed assessment submission.

Verification used Node 24.20.0 via `npm exec --yes --package=node@24.20.0 --`:

- `npm run typecheck` and `npm run build`: passed.
- `npm run test`: passed, 8 tests in 8 suites.
- `npm run test:integration`: passed, 7 tests in 5 suites against the isolated `medicon-test` MySQL/Redis services. Coverage includes API acceptance/replay/errors, schema, provider gating, receipt deduplication, exclusive claims, durable retries, deadline exhaustion and stale-lease recovery.
- `npm run lint`: failed because the configured `scripts` directory does not exist yet. Direct `npx --no-install eslint src test mock migrations` passed for all existing source/test directories; this does not make the configured lint command pass.
- `npm run test:e2e`: failed with no matching E2E tests. No pass-with-no-tests override was used.
- `docker compose -f compose.test.yaml config --quiet`: passed; `docker compose -f compose.test.yaml ps` confirmed both test services healthy. Integration tests exercised their connections and the API. The test Compose file uses upstream images without build definitions; no application image is available to build or verify yet.
- `openspec validate build-resilient-document-processing --strict --no-interactive` and `git diff --check`: passed.
- Git ignore checks confirmed `.env` and confidential PDF/DOCX inputs are excluded. A pattern scan of publishable files found no matching Gemini/GitHub/OpenAI keys or private-key headers; this is a limited scan, not a security audit.

No production behavior was changed during checkpoint preparation, and no new TDD red/green cycle is claimed. Live Gemini access, complete worker orchestration, application container startup/shutdown, and full E2E behavior remain unverified.
